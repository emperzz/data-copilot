---
description: DeerFlow 二次开发核心依据（架构边界、工程规范、环境一致性、质量门禁、upstream 同步与迭代流程）
alwaysApply: true
---

# DeerFlow 二次开发核心指导（统一执行版）

## 0. 目标与适用范围

- 本规则是 DeerFlow 二次开发的统一执行标准，适用于后端（Python/FastAPI/LangGraph）、前端（Next.js/TypeScript）与配置层。
- 所有实现以“当前仓库源码 + 当前需求”作为最终事实来源，任何文档仅用于辅助理解。

## 1. 信息来源优先级（防止误判）

执行任务时按以下优先级判断：

1. 用户当前需求与对话约束。
2. 当前仓库源码与配置文件（最终事实）。
3. `docs/architecture-overview.md`（系统级认知）。
4. `docs/feature-development-suggestions.md` 与 `mvp.md`（规划参考，不是硬约束）。
5. 其他文档与历史方案（仅用于补充思路）。

若文档与源码不一致，以源码为准。

## 2. 架构边界与放置原则（必须遵守）

### 2.1 后端边界

- Gateway API 放在 `backend/app/gateway/routers/`，只做 HTTP 协议层与参数校验，不承载复杂业务逻辑。
- 智能体、工具、状态与中间件能力放在 `backend/packages/harness/deerflow/` 下对应模块。
- 新功能优先复用现有机制：Tool、Subagent、Middleware、Memory、Skills、Custom Agent。

### 2.2 前端边界

- 页面路由放在 `frontend/src/app/`。
- 可复用 UI 放在 `frontend/src/components/`。
- 与后端交互和业务逻辑放在 `frontend/src/core/`，避免在页面组件中堆积请求逻辑。

### 2.3 配置边界

- 可配置能力优先走 `config.yaml` / `extensions_config.json`，避免将环境差异硬编码在业务代码中。
- 新工具必须进入 tool group，确保可按组隔离能力。

## 3. 功能开发的标准流程（每次都执行）

### 3.1 需求收敛

- 明确：目标用户、输入输出、约束、验收标准、非目标范围。
- 对规划文档中的功能进行“需求当下化”，不机械照搬历史路径或命名。

### 3.2 设计落盘

- 先定义契约再写实现：API schema、状态字段、工具输入输出、错误码与失败策略。
- 先选复用点：现有 router/tool/middleware/state 是否可扩展。
- 新增模块命名统一、职责单一、边界清晰。

### 3.3 实现顺序

按“数据层 -> 能力层 -> 接口层 -> UI层 -> 交互优化”推进：

1. 数据模型与存储（单一事实源）。
2. 领域工具与解析逻辑。
3. API/Agent 组装。
4. 前端展示与操作闭环。
5. 性能、容错与体验增强。

### 3.4 质量门禁（合并前必须通过）

- 后端：`cd backend && uv run pytest tests/ -v`
- 后端规范：`cd backend && uvx ruff check .`
- 前端规范：`cd frontend && pnpm lint`
- 前端类型：`cd frontend && pnpm typecheck`

任何失败都应先修复再继续开发，不跳过。

- 自 `upstream/main` 延续的开发分支在**合并 upstream 之后**，同样必须满足本节门禁（见 **§10.4**）。

## 4. 环境一致性（避免“我这能跑”）

### 4.1 统一基线

- Python 版本：3.12（以 `backend/.python-version` 为准）。
- 前端包管理：pnpm（以 `frontend/package.json` 的 `packageManager` 为准）。
- 后端包管理：uv（通过 `backend/pyproject.toml` 管理）。

### 4.2 启动方式

- 推荐统一使用仓库根目录 `make dev`。
- `make dev` 真实行为来自 `scripts/serve.sh`：
  - LangGraph：dev 模式默认启用热重载。
  - Gateway：使用 `--reload` 并包含 `*.yaml` 与 `.env` 变更监听。
  - Frontend：`next dev --turbo`。

### 4.3 依赖变更纪律

- 新增 Python 依赖：`cd backend && uv add <package>`
- 新增前端依赖：`cd frontend && pnpm add <package>`
- 依赖变化后必须验证启动与关键路径，不只验证编译通过。

### 4.4 配置与密钥

- 使用 `.env` / `config.yaml` 注入敏感信息，禁止硬编码。
- 任何新增环境变量都要同步更新示例与说明文件（如 `.env.example`）。

## 5. 编码规范（语法、可维护性、可演进）

### 5.1 通用要求

- 函数职责单一，复杂分支拆为小函数。
- 常量替代魔法值，命名体现意图和业务语义。
- 仅在必要处写“为什么”的注释，不写“代码在做什么”的注释。
- 不移除与当前任务无关的现有能力。

### 5.2 Python 侧

- 强制类型标注（参数、返回值、关键数据结构）。
- API 层输入输出使用 Pydantic schema，内部逻辑与协议层解耦。
- 异常分层：业务异常与系统异常分开处理，返回清晰错误信息。
- 使用 ruff 保持风格一致，不引入与现有代码风格冲突的格式习惯。

### 5.3 TypeScript/React 侧

- 避免 `any`，未知类型优先 `unknown` + 类型收窄。
- 组件保持纯展示与逻辑分离：页面/组件不承载重型数据编排。
- 与后端交互统一放在 `core` 层，组件通过 hook 或 service 消费。
- 保持导入顺序与 lint 规则一致（含 `import/order` 约束）。

## 6. DeerFlow 特有能力的复用策略

### 6.1 优先复用

- 需要“中断等待用户确认”：优先 `ask_clarification` + `ClarificationMiddleware`。
- 需要“复杂任务拆解”：优先启用 Subagent 与 Plan Mode。
- 需要“长期经验积累”：优先复用 Memory 机制，不重复造临时状态系统。
- 需要“固定流程指导”：优先通过 Skills/Custom Agent 定义，不把流程硬编码在分散逻辑里。

### 6.2 新能力接入

- 新工具优先通过 `config.yaml` 注册并归组，确保可配置启停与权限隔离。
- 新业务 API 走 Gateway router，并在应用入口统一注册。
- 新页面走 `workspace` 路由体系，保持导航与状态体验一致。

## 7. 面向你当前产品规划的落地节奏

按 MVP 目标，采用“先闭环、再增强”：

1. 先打通 SQL 输入 -> 解析 -> 存储 -> 查询闭环。
2. 再做冲突检测与用户确认流程，保证数据质量。
3. 再做血缘可视化与详情编辑，增强可用性。
4. 最后做自动化分析与高级能力（多跳追踪、自动任务循环、Browser 自动化）。

每阶段都要有可独立验收的演示路径，不依赖“后续再补”。

## 8. Definition of Done（完成定义）

以下条件全部满足才算完成：

- 功能路径闭环（输入、处理、输出、错误路径可验证）。
- 与现有架构边界一致，无跨层耦合扩散。
- 本地开发环境可复现，关键配置有文档说明。
- 对应 lint/typecheck/test 命令全部通过。
- 不引入无关改动，不破坏已有功能。

## 9. 明确禁止项

- 禁止依据过期文档直接编码而不核对源码。
- 禁止将临时调试逻辑、密钥、路径硬编码提交。
- 禁止跨模块复制粘贴相同业务逻辑（应抽象复用）。
- 禁止跳过验证直接宣称完成。
- 禁止在 upstream 合并存在 **Git 冲突** 或 **§10.1 语义层面高风险** 时，未经用户明确确认擅自解决冲突或选定取舍方案。

## 10. 上游（upstream）定期同步与合并

### 10.0 上游来源与历史二开分支

- **官方上游仓库**：https://github.com/bytedance/deer-flow  
- **Git 远程约定**：远程名一般为 `upstream`，跟踪分支为 **`upstream/main`**。若本地尚未配置：`git remote add upstream https://github.com/bytedance/deer-flow.git`。
- **当前从新基线延续开发的分支**：**`rewrite/from-upstream`**（自 `upstream/main` 最新节点切出，用于后续二开）。
- **此前二次开发代码所在分支**：以 **`chore/sync-upstream-20260408`** 为主线（含与 `upstream/main` 的 merge 及冲突解决记录）。Fork 的 **`main`** 可能与该线部分重合，具体以 `git log origin/main` / `git log chore/sync-upstream-20260408` 为准。

**此前二开涉及的主要能力域**（切至 `rewrite/from-upstream` 之前，已在上述分支中落地的方向；按提交主题归纳，**非穷尽**，合并时仍以 **§10.1** 的当下差异为准）：

- **数据仓库目录（dw-catalog）**：SQLite 目录存储、SQL 解析与 ingest、目录 schema 演进、物理表血缘、WITH 包裹 INSERT 的血缘修正、按语句 Markdown 备注等。
- **SQL 工具与解析**：内置 SQL 工具与文档、sqlglot 相关逻辑向 metadata 等模块的整理。
- **Memory / Skill 记忆**：Chroma 默认向量与配置、Chroma 支撑的 skill memory 工具与 repository 层等。
- **Skill**：`datawarehouse-processor` 技能文档与记忆工作流等。
- **工程与体验补丁**：后端路径（如 `.deer-flow` 在 `packages/harness` 为 cwd 时的解析）、澄清选项 JSON 字符串归一化与用户消息展示、模型工厂 `reasoning_effort` 与配置合并去重等。

若需从旧线恢复或对照实现，建议以 **`chore/sync-upstream-20260408`** 为参考分支做 **cherry-pick** 或 **分模块移植**，避免将整条 fork 历史再次整体 merge 进新基线。

---

本仓库在官方 DeerFlow 基础上二次开发，需**定期**拉取 `upstream` 并合并入当前开发分支，保持与安全修复、依赖与框架演进对齐。

**每次**准备合入 upstream 最新代码前，须先完成 **§10.1** 的检查与结论；不得在未经分析的情况下直接合并。

### 10.1 合并前检查（必须先做）

1. `git fetch upstream`（以及 `git fetch origin`，视需要）。
2. 审阅尚未合入的 upstream 提交：`git log --oneline HEAD..upstream/main`（必要时 `--stat` / 单提交 `git show`）。
3. 识别**与当前分支二次开发代码可能重叠**的变更（文件级：`git diff --name-status HEAD...upstream/main`），并对照本分支近期二开涉及模块做**影响评估**。典型高风险区包括但不限于：`backend/packages/harness/deerflow/` 核心逻辑、`backend/app/gateway/`、`frontend/src/core/` 与 workspace 相关 UI、`config.example.yaml`、`backend/**/pyproject.toml`、`backend/uv.lock`、双方都曾改过的中间件/模型工厂等。
4. **与二开是否冲突的结论**：
   - **Git 层面**：优先用只读方式预判（例如 `git merge-tree $(git merge-base HEAD upstream/main) HEAD upstream/main` 输出中是否出现冲突标记；或 `git merge --no-commit --no-ff upstream/main` 预演后 `git merge --abort`）。若工具不可用，则基于重叠文件与提交说明做风险评估。
   - **语义层面**：即使自动合并无冲突，若 upstream 修改了二开依赖的行为契约（API、配置项、状态字段、工具入参等），仍视为**高风险**，按 **§10.3** 处理而非默认可直接合并。
5. **先汇报再继续**（新增硬约束）：
   - 在执行任何实际合并动作（`git merge upstream/main`、冲突解决、提交）前，必须先向用户汇报：  
     1) 本次 upstream 新增/变更的提交与能力点；  
     2) 与当前分支的重叠文件与潜在冲突点；  
     3) Git 冲突预演结论与语义风险结论。  
   - 汇报完成后，需等待用户确认“继续后续流程”再执行下一步操作。

### 10.2 无冲突且风险可接受时

- 当 **§10.1** 结论为：预演无冲突（或等价判断），且**不会实质性破坏或覆盖当前分支的二开契约与关键路径**（重叠仅为无关模块、文档-only、或已确认对二开无行为影响），则**可直接执行** `git merge upstream/main`（合并策略以团队约定为准；保留清晰的 merge commit 信息便于追溯）。
- 即使满足“无冲突且风险可接受”，仍须先满足 **§10.1-5** 的“先汇报再继续”要求。
- 合并完成后**必须**执行 **§10.4** 验证。

### 10.3 存在冲突或高风险重叠时（须用户确认后再动手）

- **禁止**在未获用户明确指示前擅自解决冲突或选定取舍方案。
- 须向用户说明：**冲突文件列表**、**冲突原因**（例如与历史 fork 合并、同文件双端演进、依赖/锁文件分叉）、**影响程度**（运行时、测试、配置、数据路径等）以及 **2～3 条可选处理方案**（例如：以 upstream 为准并手工补回二开、以本地为准并 cherry-pick 上游补丁、分段合并等）。
- **待用户回复选定方案后**，再执行合并、解决冲突与提交。

### 10.4 合并后验证（必须执行）

merge 完成后运行质量门禁，确认合并结果可用：

- 后端：`cd backend && uv run pytest tests/ -v` 与 `cd backend && uvx ruff check .`
- 前端：`cd frontend && pnpm lint` 与 `cd frontend && pnpm typecheck`

任一项失败须先修复再继续开发；不得在未通过验证时宣称同步完成。

## 11. LLM 编码行为准则（谨慎优先）

本节用于降低常见 LLM 编码失误。默认偏向谨慎而非速度；对明显简单且低风险的小任务可按工程判断适度从简。

### 11.1 编码前先思考（Think Before Coding）

- 禁止在信息不完整时“直接假设并实现”；先明确已知与未知边界。
- 若存在不确定项，需显式写出假设；关键假设不充分时先提问再实现。
- 若需求存在多种解释，必须先给出候选解释与影响，不得静默选一种。
- 若有更简单方案，必须主动说明；对明显过度设计应提出反向建议。
- 若目标、约束或验收不清晰，必须暂停实现并指出具体歧义点。

### 11.2 简单优先（Simplicity First）

- 只实现“满足当前需求的最小代码闭环”，禁止超范围扩展。
- 禁止添加未被请求的功能、参数化能力、可配置开关或预留抽象。
- 单次使用逻辑禁止提前抽象；仅在重复或明确复用需求出现时再抽象。
- 禁止为不可能场景添加防御性分支或复杂错误处理。
- 若实现体量明显高于问题复杂度（如可显著缩短），应先重构为更小解法再提交。

### 11.3 手术式修改（Surgical Changes）

- 变更范围仅限完成当前需求所必需的代码与文件。
- 禁止顺带重构、顺带美化、顺带调整相邻注释或格式。
- 优先遵循文件既有风格与结构，避免引入局部风格漂移。
- 发现与当前任务无关的历史问题，可记录提示，但不得擅自清理。
- 仅清理“由本次修改直接引入”的孤儿代码（未使用 import、变量、函数等）；不得顺手删除既有死代码。
- 自检标准：每一处改动都应可直接映射到当前用户需求。

### 11.4 目标驱动执行（Goal-Driven Execution）

- 先将任务转成可验证目标，再进入实现。
- 缺陷修复类任务：先构造可复现失败（优先测试或最小复现），再修复至通过。
- 校验增强类任务：先定义无效输入/边界输入的验证标准，再补实现并验证。
- 重构类任务：确保重构前后行为一致，并通过既有测试或等价验证。
- 多步骤任务须给出简要执行链路：`步骤 -> 验证方式 -> 通过标准`，并按验证结果循环直至达标。
- 禁止使用“先做做看/看起来可用”作为完成标准，完成判定必须可检验。
